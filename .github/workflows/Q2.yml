name: Question2 Q2_archive_README.md
on: [push]
jobs:
  archive_readme:
    runs-on: ubuntu-latest

    steps:

      - name: Création d un fichier Q2_archive_README.md
        run: 
          cp README date --global user.name > Q2_archive_README.md
        

      - name: Push Q2_archive_README.md
        run: |
          git config --global user.name "OcsCgn"
          git config --global user.email "Oscar.cocagne@free.fr"
          git add Q2_archive_README.md
          git commit -m "Add Q2_archive_README.md"
          git push
